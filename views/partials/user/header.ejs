<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LapMart - Premium Electronics</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Cinzel:wght@700;900&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/magnific-popup.js/1.1.0/magnific-popup.min.css">
    <link href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css" rel="stylesheet">
    <style>
        /* Enhanced Scoped Header Styles */
        .header-scope {
            font-family: 'Poppins', sans-serif;
            display: block;
        }

        .header-scope * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Top Banner Styles */
        .header-scope .top-banner {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 8px 0;
            text-align: center;
            font-size: 14px;
            z-index: 1001;
        }

        .header-scope .banner-content a {
            color: #fff;
            text-decoration: underline;
            font-weight: 500;
        }

        .header-scope .close-btn {
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            font-size: 20px;
            font-weight: bold;
        }

        /* Header Styles */
        .header-scope .site-header {
            background: #ffffff;
            padding: 0 20px;
            height: 85px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            position: sticky;
            top: 0;
            z-index: 1000;
            flex-wrap: nowrap;
            gap: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        /* Logo Section */
        .header-scope .logo {
            display: flex;
            align-items: center;
            flex-shrink: 0;
            margin-right: 10px;
            transition: margin-right 0.3s ease;
        }

        .header-scope .logo.home-active {
            margin-right: 5px;
        }

        .header-scope .logo a {
            display: flex;
            align-items: center;
            text-decoration: none;
        }

        .header-scope .logo-container {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .header-scope .logo-icon {
            width: 36px;
            height: 36px;
            color: #060606;
            filter: drop-shadow(2px 2px 4px rgba(0, 0, 0, 0.5));
            transition: all 0.3s ease;
        }

        .header-scope .logo-text {
            font-family: 'Cinzel', serif;
            font-size: 41px;
            font-weight: 900;
            background: linear-gradient(to bottom, #000000 50%, #b0b0b0 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            text-transform: uppercase;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            position: relative;
        }

        .header-scope .logo-text::after {
            content: '';
            position: absolute;
            width: 50%;
            height: 2px;
            bottom: -5px;
            left: 25%;
            background: #000000;
            transition: width 0.3s ease;
        }

        .header-scope .logo a:hover .logo-icon {
            transform: scale(1.05);
            color: #d4af37;
            filter: drop-shadow(2px 2px 6px rgba(0, 0, 0, 0.6));
        }

        .header-scope .logo a:hover .logo-text {
            transform: scale(1.03);
            background: linear-gradient(to bottom, #191717 50%, #a0a0a0 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 2px 2px 6px rgba(0, 0, 0, 0.6);
        }

        .header-scope .logo a:hover .logo-text::after {
            width: 80%;
        }

        .header-scope .logo-subtext {
            font-family: 'Poppins', sans-serif;
            font-size: 10px;
            font-weight: 500;
            color: #000000;
            margin-top: 4px;
            letter-spacing: 0.5px;
        }

        /* Navigation */
        .header-scope .nav-links {
            display: flex;
            gap: 40px;
            align-items: center;
        }

        .header-scope .nav-links a {
            text-decoration: none;
            color: #000000;
            font-weight: 500;
            font-size: 16px;
            transition: color 0.3s ease;
            position: relative;
        }

        .header-scope .nav-links a.active {
            color: #d4af37;
            font-weight: 600;
        }

        .header-scope .nav-links a.active::after {
            width: 100%;
        }

        .header-scope .nav-links a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -5px;
            left: 0;
            background: #d4af37;
            transition: width 0.3s ease;
        }

        .header-scope .nav-links a:hover {
            color: #d4af37;
        }

        .header-scope .nav-links a:hover::after {
            width: 100%;
        }

        /* Search Bar */
        .header-scope .search-bar {
            display: flex;
            align-items: center;
            background: #ffffff;
            border: 2px solid #1b4848;
            border-radius: 12px;
            padding: 0 15px;
            width: 400px;
            height: 48px;
            transition: all 0.3s ease;
            position: relative;
        }

        .header-scope .search-bar:focus-within {
            border-color: #fffcf3;
            box-shadow: 0 0 0 3px rgba(22, 22, 20, 0.1);
        }

        .header-scope .search-bar input {
            flex: 1;
            border: none;
            background: transparent;
            outline: none;
            font-size: 14px;
            color: #000000;
        }

        .header-scope .search-bar input::placeholder {
            color: #666666;
        }

        .header-scope .search-bar button {
            background: none;
            border: none;
            cursor: pointer;
            color: #000000;
            padding: 5px;
            border-radius: 6px;
            transition: background-color 0.3s ease;
        }

        .header-scope .search-bar button:hover {
            background-color: rgb(254, 239, 239);
        }

        /* Right Section */
        .header-scope .cart-profile {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-right: 20px;
            flex-shrink: 0;
        }

        /* Wishlist and Cart Icons */
        .header-scope .cart-profile > a {
            color: #000000;
            text-decoration: none;
            padding: 10px;
            border-radius: 10px;
            transition: all 0.3s ease;
            position: relative;
        }

        .header-scope .cart-profile > a:hover {
            background-color: #2d3b3c;
            color: #d4af37;
        }

        /* Sign In Button */
        .header-scope .signin-btn {
            background: #d4af37;
            color: #1a2526;
            border: none;
            padding: 12px 24px;
            border-radius: 10px;
            font-weight: 600;
            font-size: 14px;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s ease;
        }

        .header-scope .signin-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(212, 175, 55, 0.3);
        }

        /* Profile Container */
        .header-scope .profile-container {
            position: relative;
            display: inline-block;
            z-index: 1002;
        }

        .header-scope .profile-button {
            background: none;
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px 12px;
            border-radius: 10px;
            transition: all 0.3s ease;
            color: #000000;
        }

        .header-scope .profile-button:hover {
            background-color: #2d3b3c;
            color: #d4af37;
        }

        .header-scope .profile-avatar {
            width: 36px;
            height: 36px;
            background: #d4af37;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #1a2526;
            font-weight: 600;
            font-size: 14px;
        }

        .header-scope .username {
            font-weight: 500;
            font-size: 14px;
        }

        /* Sidebar Styles */
        .header-scope .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            width: 280px;
            height: 100%;
            background: #ffffff;
            box-shadow: 2px 0 10px rgba(0, 0, 0, 0.2);
            transform: translateX(-100%);
            transition: transform 0.3s ease;
            z-index: 1003;
            display: flex;
            flex-direction: column;
            padding: 20px;
        }

        .header-scope .sidebar.active {
            transform: translateX(0);
        }

        .header-scope .sidebar-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.3s ease, visibility 0.3s ease;
            z-index: 1002;
        }

        .header-scope .sidebar-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .header-scope .sidebar-header {
            display: flex;
            align-items: center;
            gap: 12px;
            padding-bottom: 20px;
            border-bottom: 1px solid #e0e0e0;
        }

        .header-scope .sidebar-header .profile-avatar {
            width: 50px;
            height: 50px;
        }

        .header-scope .sidebar-menu {
            list-style: none;
            padding: 10px 0;
            flex-grow: 1;
        }

        .header-scope .sidebar-menu li {
            margin: 5px 0;
        }

        .header-scope .sidebar-menu a {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 16px;
            color: #000000;
            text-decoration: none;
            border-radius: 8px;
            transition: all 0.3s ease;
            font-size: 16px;
            font-weight: 500;
        }

        .header-scope .sidebar-menu a:hover {
            background-color: #f0f0f0;
            color: #d4af37;
        }

        .header-scope .sidebar-menu .logout-link {
            color: #e53e3e;
        }

        .header-scope .sidebar-menu .logout-link:hover {
            background-color: #ffe5e5;
            color: #c53030;
        }

        /* Mobile Menu Toggle */
        .header-scope .menu-toggle {
            display: none;
            flex-direction: column;
            cursor: pointer;
            gap: 4px;
            width: 24px;
            height: 20px;
            position: relative;
            order: -1; /* Move hamburger to the left */
        }

        .header-scope .hamburger,
        .header-scope .hamburger::before,
        .header-scope .hamburger::after {
            width: 24px;
            height: 3px;
            background: #d1d5db;
            border-radius: 2px;
            transition: all 0.3s ease;
        }

        .header-scope .hamburger {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
        }

        .header-scope .hamburger::before,
        .header-scope .hamburger::after {
            content: '';
            position: absolute;
        }

        .header-scope .hamburger::before {
            transform: translateY(-8px);
        }

        .header-scope .hamburger::after {
            transform: translateY(8px);
        }

        .header-scope .menu-toggle.active .hamburger {
            background: transparent;
        }

        .header-scope .menu-toggle.active .hamburger::before {
            transform: rotate(45deg);
            background: #d4af37;
        }

        .header-scope .menu-toggle.active .hamburger::after {
            transform: rotate(-45deg);
            background: #d4af37;
        }

        /* Responsive Design */
        @media (max-width: 1200px) {
            .header-scope .search-bar {
                width: 280px;
            }

            .header-scope .nav-links {
                gap: 25px;
            }

            .header-scope .logo-text {
                font-size: 32px;
            }

            .header-scope .logo-icon {
                width: 30px;
                height: 30px;
            }

            .header-scope .logo-subtext {
                font-size: 9px;
            }

            .header-scope .cart-profile {
                gap: 15px;
            }
        }

        @media (max-width: 1024px) {
            .header-scope .site-header {
                padding: 0 15px;
            }

            .header-scope .search-bar {
                width: 240px;
            }

            .header-scope .nav-links {
                gap: 20px;
            }

            .header-scope .logo-text {
                font-size: 30px;
            }

            .header-scope .logo-icon {
                width: 28px;
                height: 28px;
            }

            .header-scope .logo-subtext {
                font-size: 8px;
            }
        }

        @media (max-width: 900px) {
            .header-scope .nav-links {
                display: none;
            }

            .header-scope .menu-toggle {
                display: flex;
            }

            .header-scope .search-bar {
                width: 200px;
            }

            .header-scope .logo-text {
                font-size: 28px;
            }

            .header-scope .logo-icon {
                width: 26px;
                height: 26px;
            }

            .header-scope .logo-subtext {
                font-size: 7px;
            }
        }

        @media (max-width: 768px) {
            .header-scope .site-header {
                padding: 0 10px;
                height: 60px;
                gap: 8px;
            }

            .header-scope .logo-text {
                font-size: 24px;
            }

            .header-scope .logo-icon {
                width: 22px;
                height: 22px;
            }

            .header-scope .logo-subtext {
                font-size: 6px;
            }

            .header-scope .search-bar {
                width: 150px;
                height: 36px;
                padding: 0 8px;
            }

            .header-scope .search-bar input {
                font-size: 12px;
            }

            .header-scope .cart-profile {
                gap: 8px;
            }

            .header-scope .cart-profile > a {
                padding: 6px;
            }

            .header-scope .cart-profile > a svg {
                width: 20px;
                height: 20px;
            }

            .header-scope .menu-toggle {
                width: 22px;
                height: 18px;
            }

            .header-scope .hamburger,
            .header-scope .hamburger::before,
            .header-scope .hamburger::after {
                width: 22px;
                height: 2.5px;
            }

            .header-scope .hamburger::before {
                transform: translateY(-7px);
            }

            .header-scope .hamburger::after {
                transform: translateY(7px);
            }
        }

        @media (max-width: 480px) {
            .header-scope .site-header {
                padding: 0 5px;
                height: 50px;
                gap: 4px;
                justify-content: space-between;
            }

            .header-scope .logo {
                flex: 0 0 auto;
            }

            .header-scope .logo-text {
                font-size: 20px;
            }

            .header-scope .logo-icon {
                width: 18px;
                height: 18px;
            }

            .header-scope .logo-subtext {
                display: none;
            }

            .header-scope .search-bar {
                width: 40px;
                height: 32px;
                padding: 0;
                background: transparent;
                border: none;
                position: relative;
                transition: width 0.3s ease, background 0.3s ease;
            }

            .header-scope .search-bar input {
                display: none;
            }

            .header-scope .search-bar button {
                width: 100%;
                height: 100%;
                padding: 0;
                display: flex;
                align-items: center;
                justify-content: center;
            }

            .header-scope .search-bar svg {
                width: 18px;
                height: 18px;
            }

            .header-scope .search-bar.active {
                width: 120px;
                background: #ffffff;
                border: 2px solid #1b4848;
                padding: 0 6px;
            }

            .header-scope .search-bar.active input {
                display: block;
                font-size: 11px;
            }

            .header-scope .cart-profile {
                gap: 4px;
                flex: 0 0 auto;
            }

            .header-scope .cart-profile > a {
                padding: 4px;
            }

            .header-scope .cart-profile > a svg {
                width: 16px;
                height: 16px;
            }

            .header-scope .signin-btn {
                padding: 5px 8px;
                font-size: 10px;
                gap: 2px;
            }

            .header-scope .signin-btn svg {
                width: 10px;
                height: 10px;
            }

            .header-scope .profile-button {
                padding: 3px 5px;
            }

            .header-scope .profile-avatar {
                width: 28px;
                height: 28px;
                font-size: 10px;
            }

            .header-scope .top-banner {
                font-size: 12px;
                padding: 5px 0;
            }

            .header-scope .close-btn {
                right: 15px;
                font-size: 18px;
            }
        }

        .profile-avatar {
            width: 40px;
            height: 40px;
            background-color: #ddd;
            border-radius: 50%;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 8px;
        }

        .avatar-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 50%;
        }

        .avatar-letter {
            font-size: 18px;
            font-weight: bold;
            color: #444;
        }
    </style>
</head>
<body>
    <div class="header-scope">
        <header class="site-header">
            <!-- Mobile menu toggle -->
            <div class="menu-toggle">
                <div class="hamburger"></div>
            </div>

            <!-- Logo -->
            <div class="logo">
                <a href="/">
                    <div class="logo-container">
                        <svg class="logo-icon" xmlns="http://www.w3.org/2000/svg" width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <rect x="2" y="7" width="20" height="10" rx="2" ry="2"></rect>
                            <path d="M16 21H8a2 2 0 0 1-2-2v-1h12v1a2 2 0 0 1-2 2z"></path>
                            <line x1="12" y1="7" x2="12" y2="3"></line>
                        </svg>
                        <span class="logo-text">LapMart</span>
                    </div>
                </a>
            </div>

            <!-- Navigation -->
            <nav class="nav-links">
                <a href="/" class="nav-link" id="home">Home</a>
                <a href="/shop" class="nav-link" id="shop">Shop</a>
                <a href="/contacts" class="nav-link" id="contacts">Contacts</a>
                <a href="/about" class="nav-link" id="About">About</a>
            </nav>

            <!-- Search Bar
            <div class="search-bar">
                <input type="text" placeholder="Search for products..." aria-label="Search">
                <button type="submit" aria-label="Search button">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <circle cx="11" cy="11" r="8"></circle>
                        <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
                    </svg>
                </button>
            </div> -->

            <!-- Cart and Profile Section -->
            <div class="cart-profile">
                <a href="/wishlist" aria-label="Wishlist" id="Wishlist-btn">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
                    </svg>
                </a>
                <a href="/cart" aria-label="Cart" id="cart-btn">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <circle cx="9" cy="21" r="1"></circle>
                        <circle cx="20" cy="21" r="1"></circle>
                        <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path>
                    </svg>
                </a>

                <% if (locals.user) { %>
                    <div class="profile-container">
                        <button class="profile-button" id="profileButton" aria-label="Profile">
                            <div class="profile-avatar">
                                <% if (user.profileImage && user.profileImage[0]) { %>
                                    <img src="<%= user.profileImage[0] %>" alt="Profile Image" class="avatar-img" />
                                <% } else { %>
                                    <span class="avatar-letter"><%= user.name.charAt(0).toUpperCase() %></span>
                                <% } %>
                            </div>
                            <span class="username"><%= user.name %></span>
                        </button>
                    </div>
                <% } else { %>
                    <a href="/login" class="signin-btn">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4"></path>
                            <polyline points="10 17 15 12 10 7"></polyline>
                            <line x1="15" y1="12" x2="3" y2="12"></line>
                        </svg>
                        Sign In
                    </a>
                <% } %>
            </div>
        </header>

        <!-- Sidebar -->
        <div class="sidebar" id="sidebar">
            <% if (locals.user) { %>
                <div class="sidebar-header">
                    <div class="profile-avatar">
                        <% if (user.profileImage && user.profileImage[0]) { %>
                            <img src="<%= user.profileImage[0] %>" alt="Profile Image" class="avatar-img" />
                        <% } else { %>
                            <span class="avatar-letter"><%= user.name.charAt(0).toUpperCase() %></span>
                        <% } %>
                    </div>
                    <div class="profile-info">
                        <h4><%= user.name %></h4>
                        <p><%= user.email %></p>
                    </div>
                </div>
            <% } else { %>
                <div class="sidebar-header">
                    <a href="/login" class="signin-btn" style="width: 100%; justify-content: center;">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4"></path>
                            <polyline points="10 17 15 12 10 7"></polyline>
                            <line x1="15" y1="12" x2="3" y2="12"></line>
                        </svg>
                        Sign In
                    </a>
                </div>
            <% } %>
            <ul class="sidebar-menu">
                <li><a href="/"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path><polyline points="9 22 9 12 15 12 15 22"></polyline></svg> Home</a></li>
                <li><a href="/shop"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path><line x1="3" y1="6" x2="21" y2="6"></line><path d="M16 10a4 4 0 0 1-8 0"></path></svg> Shop</a></li>
                <li><a href="/contacts"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 2L11 13"></path><path d="M22 2l-7 20-4-9-9-4 20-7z"></path></svg> Contacts</a></li>
                <li><a href="/about"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="16" x2="12" y2="12"></line><line x1="12" y1="8" x2="12" y2="8"></line></svg> About</a></li>
                <% if (locals.user) { %>
                    <li><a href="/profile"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path><circle cx="12" cy="7" r="4"></circle></svg> Profile</a></li>
                    <li><a href="/wishlist"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path></svg> Wishlist</a></li>
                    <li><a href="/view-order"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path><line x1="3" y1="6" x2="21" y2="6"></line><path d="M16 10a4 4 0 0 1-8 0"></path></svg> My Orders</a></li>
                    <li><a href="/address"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path><circle cx="12" cy="10" r="3"></circle></svg> Address</a></li>
                    <li><a href="/wallet"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12V7H5a2 2 0 0 1 0-4h16v4"></path><path d="M3 5v14a2 2 0 0 0 2 2h16v-4"></path><path d="M18 12a2 2 0 0 0 0 4h3"></path></svg> Wallet</a></li>
                    <li><a href="/logout" class="logout-link"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path><polyline points="16 17 21 12 16 7"></polyline><line x1="21" y1="12" x2="9" y2="12"></line></svg> Logout</a></li>
                <% } %>
            </ul>
        </div>
        <div class="sidebar-overlay" id="sidebarOverlay"></div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const cartBtn = document.getElementById('cart-btn');

            if (cartBtn) {
                cartBtn.addEventListener('click', async (e) => {
                    e.preventDefault();

                    try {
                        const res = await fetch('/check-session', {
                            method: 'GET',
                            headers: {
                                'Accept': 'application/json'
                            }
                        });

                        const data = await res.json();

                        if (data.loggedIn) {
                            window.location.href = '/cart';
                        } else {
                            Swal.fire({
                                icon: 'warning',
                                title: 'Login Required',
                                text: 'Please login to view your cart',
                                confirmButtonText: 'OK'
                            });
                        }
                    } catch (error) {
                        console.error('Error checking session:', error);
                        Swal.fire({
                            icon: 'error',
                            title: 'Something went wrong',
                            text: 'Unable to access cart. Try again later.'
                        });
                    }
                });
            }

            document.getElementById('Wishlist-btn').addEventListener('click', async function (e) {
                e.preventDefault();

                try {
                    const res = await fetch('/wishlist', {
                        method: 'GET',
                        headers: { 'Accept': 'application/json' }
                    });

                    const data = await res.json();

                    if (res.ok) {
                        window.location.href = '/wishlist';
                    } else {
                        Swal.fire({
                            icon: 'warning',
                            title: 'Login Required',
                            text: 'Please login to view your wishlist',
                            confirmButtonText: 'OK'
                        });
                    }
                } catch (error) {
                    console.error('Error checking wishlist:', error);
                    Swal.fire({
                        icon: 'error',
                        title: 'Something went wrong',
                        text: 'Unable to access wishlist. Try again later.'
                    });
                }
            });

            (function() {
                // Utility: Debounce function for resize events
                const debounce = (func, wait) => {
                    let timeout;
                    return (...args) => {
                        clearTimeout(timeout);
                        timeout = setTimeout(() => func.apply(null, args), wait);
                    };
                };

                // Sidebar toggle
                const menuToggle = document.querySelector('.header-scope .menu-toggle');
                const sidebar = document.getElementById('sidebar');
                const sidebarOverlay = document.getElementById('sidebarOverlay');

                if (menuToggle && sidebar && sidebarOverlay) {
                    menuToggle.addEventListener('click', (e) => {
                        e.preventDefault();
                        menuToggle.classList.toggle('active');
                        sidebar.classList.toggle('active');
                        sidebarOverlay.classList.toggle('active');
                    });

                    sidebarOverlay.addEventListener('click', () => {
                        menuToggle.classList.remove('active');
                        sidebar.classList.remove('active');
                        sidebarOverlay.classList.remove('active');
                    });

                    // Close sidebar when clicking a link
                    const sidebarLinks = sidebar.querySelectorAll('.sidebar-menu a');
                    sidebarLinks.forEach(link => {
                        link.addEventListener('click', () => {
                            menuToggle.classList.remove('active');
                            sidebar.classList.remove('active');
                            sidebarOverlay.classList.remove('active');
                        });
                    });
                }

                // Profile button opens sidebar on mobile
                const profileButton = document.getElementById('profileButton');
                if (profileButton) {
                    profileButton.addEventListener('click', (e) => {
                        e.preventDefault();
                        if (window.innerWidth <= 900) {
                            menuToggle.classList.toggle('active');
                            sidebar.classList.toggle('active');
                            sidebarOverlay.classList.toggle('active');
                        }
                    });
                }

                // Navigation active state
                const logo = document.querySelector('.header-scope .logo');
                const navLinks = document.querySelectorAll('.header-scope .nav-link');
                navLinks.forEach(link => {
                    link.addEventListener('click', function(e) {
                        navLinks.forEach(l => l.classList.remove('active'));
                        this.classList.add('active');
                        if (this.getAttribute('href') === '/') {
                            logo.classList.add('home-active');
                        } else {
                            logo.classList.remove('home-active');
                        }
                    });
                });

                if (window.location.pathname === '/') {
                    const homeLink = document.querySelector('.header-scope a[href="/"]');
                    if (homeLink) {
                        homeLink.classList.add('active');
                        logo.classList.add('home-active');
                    }
                }

                // Search bar toggle for mobile
                const searchBar = document.querySelector('.header-scope .search-bar');
                const searchToggle = document.querySelector('.header-scope .search-bar button');
                if (searchBar && searchToggle) {
                    searchToggle.addEventListener('click', (e) => {
                        e.preventDefault();
                        if (window.innerWidth <= 480) {
                            searchBar.classList.toggle('active');
                        }
                    });
                }

                // Responsive: Debounced resize handler
                window.addEventListener('resize', debounce(() => {
                    if (window.innerWidth > 480 && searchBar && searchBar.classList.contains('active')) {
                        searchBar.classList.remove('active');
                    }
                    if (window.innerWidth > 900 && sidebar && sidebarOverlay) {
                        sidebar.classList.remove('active');
                        sidebarOverlay.classList.remove('active');
                        menuToggle.classList.remove('active');
                    }
                }, 100));
            })();
        });
    </script>
</body>
</html>